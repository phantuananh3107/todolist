# Frontend Flutter Skeleton

## Run
1. Install Flutter SDK
2. Run `flutter pub get`
3. Run `flutter run`

## Notes
- `.env` stores frontend config only.
- Statistics is opened from Task flow, not a bottom tab.
- This is a starter skeleton for the mobile app in the project brief.
